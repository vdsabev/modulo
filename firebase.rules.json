{
  "rules": {
    ".read": true,
    ".write": false,

    // Posts
    "posts": {
      "$post": {
        "title": { ".validate": "newData.isString()" },
        "content": { ".validate": "newData.isString()" }
      }
    }
  }
}
